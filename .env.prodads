# Loaded automatically by Vite when running:
#   vite build --mode prodads
# (via npm run build:prod-ads)
#
# Switches the AdMob hook to use REAL production ad unit IDs.
# Default builds (npm run build) use Google's TEST ad units instead,
# so you never accidentally serve real ads to your own dev device.
VITE_AD_MODE=prod
